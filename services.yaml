apiVersion: v1
kind: Service
metadata:
  name: capstoneproject2-service
spec:
  type: NodePort
  selector:
    app = apacheapp
  ports:
    - port: 80
      # By default and for convenience, the `targetPort` is set to
      # the same value as the `port` field.
      targetPort: 80
      
      # will allocate a port from a range (default: 30000-32767)
      nodePort: 30008
