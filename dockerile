FROM ubuntu:latest

# Install Apache
RUN apt-get update && \
    apt-get install -y apache2

# Copy contents to Apache document root
COPY . /var/www/html

# Expose port 80
EXPOSE 80

# Start Apache in the foreground
CMD ["apache2ctl", "-D", "FOREGROUND"]
